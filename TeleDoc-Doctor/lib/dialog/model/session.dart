class session {
  final String fake_name;
  final String date_time;

  // Constructor
  const session({this.fake_name, this.date_time});

  /* session.fromJson(Map json)
      : fake_name = json['fake_name'],
        date_time = json['date_time'];
*/
}
